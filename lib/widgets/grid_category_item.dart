import 'package:flutter/material.dart';

class GridCategoryItem extends StatelessWidget {
  const GridCategoryItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
